{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": ".railway/Dockerfile",
    "buildContext": ".railway"
  },
  "deploy": {
    "healthcheckPath": "/health"
  }
}
