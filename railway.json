{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd .railway && npm install --omit=dev"
  },
  "deploy": {
    "startCommand": "cd .railway && npm start",
    "healthcheckPath": "/health"
  }
}
