{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": ".railway/Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/health"
  }
}
